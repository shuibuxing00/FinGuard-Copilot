# Splunk MCP Server package (optional)

Download from Splunkbase and save here as `splunk-mcp-server.tgz`:

https://splunkbase.splunk.com/app/7931

Or run: `py scripts/install_splunk_mcp.py`

The `.tgz` file is not committed to git (install from Splunkbase locally).
